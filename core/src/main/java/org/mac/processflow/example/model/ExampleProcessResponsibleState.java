package org.mac.processflow.example.model;

import org.mac.processflow.model.ProcessResponsibleState;

public enum ExampleProcessResponsibleState implements ProcessResponsibleState {

	RESPONSIBLE, EXECUTOR, IMPLEMENTOR
}
